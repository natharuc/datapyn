# DataPyn v2 — VS Code extension

Extensão TypeScript para o fork VS Code: comandos Run Block, painéis (conexões, schema, variáveis, resultados), language association para `.dpw`.

Comunicação com o motor em `../runtime/` via JSON-RPC (stdio).

**Não iniciado** — scaffold para Fase 0/2 do plano de migração.
