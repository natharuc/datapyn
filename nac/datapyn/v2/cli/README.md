# DataPyn v2 — CLI

Ferramentas de linha de comando:

- `datapyn migrate` — importar `~/.datapyn/` → pasta de projeto (`.datapyn/` + arquivos `.dpw`)
- `datapyn run` (opcional) — executar `.dpw` headless / CI

**Não iniciado** — scaffold para Fase 2 do plano de migração.
