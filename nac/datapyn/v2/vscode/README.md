# DataPyn v2 — VS Code fork

Submódulo ou clone do [vscode](https://github.com/microsoft/vscode) (Code-OSS) com `product.json` DataPyn e extensões bundled (`../extension/`).

Build e merge upstream documentados aqui quando o fork for adicionado.

**Não iniciado** — scaffold para Fase 0/3 do plano de migração.
